It occurs to me that we don't actually load operators based on the data model. At least not exclusively. We really load operators based on what is in the dynamic vocabularies, and those are determined by the database which we are using ConceptQL with. And so we can't load operators into ConceptQL until we know what database we are dealing with. And that's only partially true. Um off the top of my head, it seems like a database's vocabulary information is ignored in the presence of lexicon. Also we have non-dynamic operators like one and two out or before and after and those need only be loaded once. It would be a waste if we reloaded them multiple times. We also have operators stored CSV files both for multiple vocabularies and just well known vocabularies. like CSV-9, HCPCS CPT um, reversed respectively. And those can be loaded once. I'm not really sure what the data model portion is doing at this point. Data models would be like GDM versus Odyssey. I'm not sure if those even are relevant. But it seems like what we would want to do is load all of the quote static operators once. And then each time a database is loaded, assuming lexicon doesn't exist. then we would create dynamic vocabularies based on a database on a per database level. If this turns out to be really slow, we can always cache based on the database connection op uh the SQL connection object, maybe. Or like a hashed Let's just not cache it right now. Caching is always a pain. But I think that situation only arises when there is no lexicon. And now that I think about this a little bit more, all of this is in response to the fact that, for whatever reason, Docker doesn't seem to be able to connect to a lexicon database. which is a Docker container that should already be up and running. So I wonder if there's a timing issue there. I mean, this code has worked for us forever, and admittedly it is a flawed representation of Like the the architecture of ConceptQL clearly is flawed because there is truly multiple sets of operators that could possibly exist in a single run of ConceptQL But we aren't supporting that right now.